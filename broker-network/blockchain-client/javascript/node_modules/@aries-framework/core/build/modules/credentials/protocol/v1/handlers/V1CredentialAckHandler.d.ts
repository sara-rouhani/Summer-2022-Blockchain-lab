import type { Handler, HandlerInboundMessage } from '../../../../../agent/Handler';
import type { V1CredentialService } from '../V1CredentialService';
import { V1CredentialAckMessage } from '../messages';
export declare class V1CredentialAckHandler implements Handler {
    private credentialService;
    supportedMessages: (typeof V1CredentialAckMessage)[];
    constructor(credentialService: V1CredentialService);
    handle(messageContext: HandlerInboundMessage<V1CredentialAckHandler>): Promise<void>;
}
