import type { IndyLedgerService } from '../../../ledger';
import type { DidResolver } from '../../domain/DidResolver';
import type { ParsedDid, DidResolutionResult } from '../../types';
export declare class SovDidResolver implements DidResolver {
    private indyLedgerService;
    constructor(indyLedgerService: IndyLedgerService);
    readonly supportedMethods: string[];
    resolve(did: string, parsed: ParsedDid): Promise<DidResolutionResult>;
    private processEndpointTypes;
    private addServices;
}
