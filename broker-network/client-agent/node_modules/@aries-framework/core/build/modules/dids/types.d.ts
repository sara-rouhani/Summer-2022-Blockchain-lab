import type { DidDocument } from './domain';
import type { DIDResolutionOptions, ParsedDID, DIDDocumentMetadata, DIDResolutionMetadata } from 'did-resolver';
export declare type ParsedDid = ParsedDID;
export declare type DidResolutionOptions = DIDResolutionOptions;
export declare type DidDocumentMetadata = DIDDocumentMetadata;
export interface DidResolutionMetadata extends DIDResolutionMetadata {
    message?: string;
}
export interface DidResolutionResult {
    didResolutionMetadata: DidResolutionMetadata;
    didDocument: DidDocument | null;
    didDocumentMetadata: DidDocumentMetadata;
}
