import type { Handler, HandlerInboundMessage } from '../../../../../agent/Handler';
import type { V2CredentialService } from '../V2CredentialService';
import { V2CredentialProblemReportMessage } from '../messages/V2CredentialProblemReportMessage';
export declare class V2CredentialProblemReportHandler implements Handler {
    private credentialService;
    supportedMessages: (typeof V2CredentialProblemReportMessage)[];
    constructor(credentialService: V2CredentialService);
    handle(messageContext: HandlerInboundMessage<V2CredentialProblemReportHandler>): Promise<void>;
}
