import type { Handler, HandlerInboundMessage } from '../../../../../agent/Handler';
import type { V1CredentialService } from '../V1CredentialService';
import { V1CredentialProblemReportMessage } from '../messages';
export declare class V1CredentialProblemReportHandler implements Handler {
    private credentialService;
    supportedMessages: (typeof V1CredentialProblemReportMessage)[];
    constructor(credentialService: V1CredentialService);
    handle(messageContext: HandlerInboundMessage<V1CredentialProblemReportHandler>): Promise<void>;
}
