import type { Handler, HandlerInboundMessage } from '../../../../../agent/Handler';
import type { V2CredentialService } from '../V2CredentialService';
import { V2CredentialAckMessage } from '../messages/V2CredentialAckMessage';
export declare class V2CredentialAckHandler implements Handler {
    private credentialService;
    supportedMessages: (typeof V2CredentialAckMessage)[];
    constructor(credentialService: V2CredentialService);
    handle(messageContext: HandlerInboundMessage<V2CredentialAckHandler>): Promise<void>;
}
